/// --- SEARCHBAR JSX --- ///
import React from "react";

import "./SearchBar.scss";

const SearchBar = () => {
	return <input type="text" className="search" placeholder="Search..." />;
};

export default SearchBar;
